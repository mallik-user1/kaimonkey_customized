 version: 2.1
 jobs:
    scan_check:
        docker:
          - image: alpine:3.7
        steps:
          - run:
              command: pwd 
          - run:
              name: zscanner check
              command: ./zscanner.sh
 workflows:
  build_and_test:
    jobs:
      - scan_check
